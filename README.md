### Jquery_practica1
```
<p> este repositirio es generado para acompañar la practica de los aprendices de 4 trimestre </p>
```
``` let peticion= (e)=>{
.ajax({
      url:'servidor',
      method: 'POST',
      dataType:'json',
      data,
      success: ()=> {
       console.log(data)
      }
})
}
```
